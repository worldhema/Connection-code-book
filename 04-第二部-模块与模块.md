# 第二部　模块与模块 —— 功能的归类

---

## 2.1　断点：几十个函数堆在一起，找不着了

上一章结尾，我们给 CoinJar 拆出了几个会互相喊话的函数，它从一块铁板变成了一张小网。那时它还很清爽。

可清爽是暂时的。我们快进几个月，看看一个"长大中"的 CoinJar 变成了什么样——它现在有了三十多个函数，全都平铺在一起：

```
函数 解析输入(...)
函数 把文字转成数字(...)
函数 解析日期(...)
函数 解析币种(...)
函数 解析备注(...)
函数 检查账目(...)
函数 检查金额(...)
函数 检查日期(...)
函数 记录账目(...)
函数 算服务费(...)
函数 算总额(...)
函数 算月均(...)
函数 报告结果(...)
函数 显示成表格(...)
函数 显示成图表(...)
函数 格式化金额(...)
函数 格式化日期(...)
... # 还有十几个
```

每个函数本身都没问题。可它们**堆在一起**这件事，本身就成了问题。

> **几十个函数平铺在一起，会带来什么麻烦？**

- **找不着**。你想改"怎么算月均"，得在三十多个名字里一行行扫过去找 `算月均`。
- **看不清关系**。`解析日期` 和 `格式化日期` 是一伙的吗？`算服务费` 和 `算总额` 该归一类吗？光看这张平铺的清单，根本看不出**谁和谁是亲戚**。
- **容易撞车**。负责"解析"的人想加一个叫 `检查` 的小函数，结果发现已经有人用了这名字——明明两个 `检查` 干的是八竿子打不着的事。

注意，这一章的断点和上一章不一样。上一章的问题是"一段代码太长"，解药是**拆**。
这一章的问题恰恰相反——**拆得太碎、散了一地**，解药是**归类**。

我们需要把这些散落的函数，按它们"是干哪一类活的"，**收进不同的抽屉**。

这个"抽屉"，就叫**模块**。

---

## 2.2　比喻：把工具按用途装进不同的抽屉

想象一个木工的工作台。

新手的工作台，是把所有工具一股脑摊在桌面上：锤子、钉子、螺丝刀、卷尺、锯子、砂纸……全平铺着。要用的时候，满桌乱翻。

老师傅的工作台，是一排**带标签的抽屉**：

- "**测量**"抽屉：卷尺、角尺、铅笔；
- "**切割**"抽屉：锯子、美工刀；
- "**打磨**"抽屉：砂纸、锉刀；
- "**连接**"抽屉：锤子、钉子、螺丝、螺丝刀。

好处是立竿见影的：

- 要测量，**直接拉开"测量"抽屉**，不用满桌找；
- 一眼就知道**哪些工具是一伙的**——同一个抽屉里的，就是干同类活的；
- "切割"抽屉里有把叫"小刀"的，"打磨"抽屉里也能有把叫"小刀"的，**两个抽屉各论各的，互不打架**。

把这套搬到代码里：

> **模块，就是程序里那一个个"带标签的抽屉"。**
> 我们把功能相近的函数，收进同一个抽屉，给抽屉起个名字（比如"解析"抽屉、"显示"抽屉）。

抽屉与抽屉之间，不再是"一堆函数挤在一起"，而是"几个各司其职的部门"。CoinJar 内部的连接，从"函数 ↔ 函数"的细线，第一次升级成了"部门 ↔ 部门"的粗线。

---

## 2.3　模块是什么：一组相关函数的"打包"

把 2.1 节那张平铺的清单，按用途归一下类，模块就自然浮现出来了：

```
模块 解析:               # "解析"抽屉
    解析输入(...)
    把文字转成数字(...)
    解析日期(...)
    解析币种(...)
    解析备注(...)

模块 检查:               # "检查"抽屉
    检查账目(...)
    检查金额(...)
    检查日期(...)

模块 计算:               # "计算"抽屉
    算服务费(...)
    算总额(...)
    算月均(...)

模块 显示:               # "显示"抽屉
    报告结果(...)
    显示成表格(...)
    显示成图表(...)
    格式化金额(...)
    格式化日期(...)
```

同样三十多个函数，现在被收进了四个抽屉。变化是质的：

- 你想改"月均怎么算"——直接去 `计算` 模块，范围一下从"三十多个函数"缩小到"三个函数"。
- 你想知道"显示相关的有哪些能力"——拉开 `显示` 抽屉，一目了然。
- `解析` 模块里可以有个叫 `检查` 的内部小函数，`检查` 模块里也可以有——**它们分属不同抽屉，不会撞名**（这一点 2.7 节细说）。

所以，模块的定义可以很朴素：

> **模块 = 一组相关函数（以及它们共用的数据）的打包，外加一个名字。**

它比函数大一号。函数收纳的是"语句"，模块收纳的是"函数"。这是我们这趟电梯往上爬的第一层——**收纳单位变大了。**

---

## 2.4　导入与导出：一个模块怎么把能力借给另一个模块

抽屉分好了，但马上有个现实问题：

`显示` 抽屉在拼那句"已记录：午饭 18 元"时，需要把金额格式化成漂亮的样子（比如 `¥18.00`）。可"格式化金额"这本事……到底该归谁？

假设我们把 `格式化金额` 放在 `显示` 抽屉里。那如果 `计算` 模块也想用它呢？**一个抽屉里的工具，另一个抽屉的人能不能借？**

能。但要讲规矩。这个规矩，就是**导出**（export）和**导入**（import）。

- **导出**：一个模块主动声明——"我这个抽屉里，**这几样工具是对外开放、欢迎来借的**。"
- **导入**：另一个模块声明——"我要**从那个抽屉，借用某样工具**。"

我们把 `格式化金额` 单独提一个公共抽屉 `格式化`，让它对外开放：

```
模块 格式化:
    导出 格式化金额(金额):       # ← 声明：这个工具对外开放
        返回 "¥" + 保留两位小数(金额)

    导出 格式化日期(日期):       # ← 这个也开放
        返回 ...

    保留两位小数(数字):          # ← 没写"导出"，这是抽屉内部自用的，不外借
        返回 ...
```

然后 `显示` 模块来借：

```
模块 显示:
    从 格式化 导入 格式化金额      # ← 声明：我要用"格式化"抽屉里的"格式化金额"

    报告结果(账目):
        漂亮金额 = 格式化金额(账目["金额"])   # ← 借来直接用
        显示("已记录：" + 账目["项目"] + " " + 漂亮金额)
```

请你品味这套机制的精妙。它做到了两件事：

1. **建立了连接**。`显示` 通过一行 `导入`，正式接上了 `格式化`——这就是**模块与模块之间的连线**。它和上一章函数间的"调用"是同一种思想（A 借用 B 的能力），但发生在更大的尺度上。

2. **划清了边界**。注意 `格式化` 里那个没写"导出"的 `保留两位小数`——它是抽屉的**内部私货**，外人借不到，也不该惦记。模块因此有了**内外之分**：对外开放的是"门面"，藏在里面的是"后厨"。

> 导出和导入，就像在两个抽屉之间，**拉了一根明确的借用专线**：
> 谁借了谁、借的是哪样东西，一行字写得清清楚楚。

这根"明线"极其宝贵。因为有了它，你只要看一个模块**导入了什么**，就能立刻知道**它依赖谁、离不开谁**——这正是下一节要讲的。

---

## 2.5　接口与实现：你只需要知道"怎么用"，不必知道"怎么做"

再看一眼上一节 `显示` 借用 `格式化金额` 的那一刻：

```
漂亮金额 = 格式化金额(账目["金额"])
```

请注意一件你可能忽略了的事：**`显示` 模块在用 `格式化金额` 的时候，完全不知道、也完全不关心它内部是怎么实现的。**

`格式化金额` 内部是用了 `保留两位小数`？还是调了某个数学库？还是查了张表？——`显示` 一概不知，也**一概不需要知道**。它只知道一份"使用说明"：

> **给我一个数字，我还你一个带 ¥、带两位小数的字符串。**

这份"使用说明"，就叫**接口**（interface）；而藏在它背后的真正代码，叫**实现**（implementation）。

这组区分，是整个软件世界里最重要的概念之一，值得我们用一个生活例子焊进脑子：

> 你家墙上的**插座**，就是一个接口。
> 它的"使用说明"是："两个孔，插进去给你 220 伏电。"
> 至于这电是火电、水电还是核电，电是怎么从发电厂一路送到你家的——
> 你**完全不需要知道**。你只管插。
>
> 这就是接口的威力：**它把"怎么用"和"怎么做"彻底分开了。**
> 插座（接口）保持不变，背后的发电方式（实现）可以随便换——你家的电器一个都不用动。

放回 CoinJar：

- 哪天我们想把金额格式从 `¥18.00` 改成 `18.00 元`，只要改 `格式化金额` 的**实现**，**接口不变**（还是"给数字、还字符串"），那么所有借用它的模块——`显示`、`计算`、未来的任何模块——**一行都不用改**。
- 反过来，只要接口稳定，`格式化` 模块的作者就可以放心大胆地优化内部，不必担心牵连别人。

> **接口是模块对外许下的"承诺"，实现是它私下兑现承诺的"做法"。**
> 连接，永远是接在"承诺"上的，而不是接在"做法"上的。

这就是为什么模块化能让大程序变得可控：**每个模块只需对外暴露一小块稳定的接口，把一大堆易变的实现藏在身后。** 模块之间靠接口相连，于是一边改动，不会震塌另一边。

---

## 2.6　依赖：模块之间的"谁离不开谁"

当 `显示` 导入了 `格式化`，我们就说：

> **`显示` 依赖 `格式化`。**

"依赖"是个朴素的词，意思就是"离不开"：没有 `格式化`，`显示` 就跑不起来。这是模块之间连接的**方向性**——A 依赖 B，是一条**从 A 指向 B 的箭头**。

把 CoinJar 所有模块的依赖箭头画出来，会得到一张图，叫**依赖图**：

```
        ┌─────────┐
        │ 总指挥   │          （记一笔账的总流程）
        └────┬────┘
     ┌───────┼───────┬──────────┐
     ▼       ▼       ▼          ▼
  ┌──────┐┌──────┐┌──────┐  ┌──────┐
  │ 解析 ││ 检查 ││ 计算 │  │ 显示 │
  └──┬───┘└──────┘└──┬───┘  └──┬───┘
     │               │         │
     └───────┬───────┴─────────┘
             ▼
        ┌─────────┐
        │ 格式化   │          （最底层，谁都可能用到，它谁都不依赖）
        └─────────┘
```

这张图，就是 CoinJar 的**骨架结构**。会读它，你就能一眼看出很多事：

- **谁是顶梁柱**：`总指挥` 在最上面，它依赖了几乎所有人——它是"组织者"。
- **谁是基石**：`格式化` 在最下面，很多人依赖它，但它**谁也不依赖**——它是稳定的"地基"，越底层的东西越该稳定。
- **改动的波及范围**：如果你改了 `格式化` 的**接口**（注意是接口，不是实现），那么所有指向它的箭头——`解析`、`计算`、`显示`——**都可能受影响**。依赖图，就是改动的"地震波及范围图"。

这里还藏着一条重要的设计纪律，请你记住：

> **依赖的箭头，最好都朝一个方向流，不要绕圈。**

什么叫绕圈？比如 `显示` 依赖 `计算`，`计算` 又反过来依赖 `显示`——箭头首尾相接绕成了环，这叫**循环依赖**。它很糟糕，因为：

- 你说不清"谁先谁后"——它俩谁也离不开谁，像两个人互相搀扶才能站住，一个倒下另一个也跟着倒；
- 没法单独测试、单独理解、单独替换任何一个。

好的模块结构，依赖箭头应该像水一样**从高处流向低处**：上层（贴近业务的 `总指挥`）依赖下层（通用的 `格式化`），而不会倒流。这样整个程序就有了清晰的**层次**——这一点，到第四部讲目录结构时，我们会再次相遇。

---

## 2.7　命名空间：避免两个抽屉里东西重名打架

回到 2.2 节那个伏笔："切割"抽屉里有把"小刀"，"打磨"抽屉里也能有把"小刀"，互不打架。这是怎么做到的？

靠的是**命名空间**（namespace）。这个词听着唬人，意思其实很简单：

> **每个模块，都为自己框出了一小块"专属的名字地盘"。**
> 同一个名字，在不同的地盘里，指的是不同的东西。

还记得吗？`解析` 模块里可以有个内部小函数叫 `检查`，`检查` 模块里也有自己的函数——它们不会冲突。因为当你要精确地指明用哪个时，你会带上**抽屉的名字**：

```
解析.检查(...)      # 这是"解析"抽屉里的那个检查
检查.检查账目(...)   # 这是"检查"抽屉里的
```

`模块名.函数名` 这种写法，就像人的"姓 + 名"。光叫"小明"会重，但"王小明"和"李小明"就分得清了。**模块名就是那个"姓"，它给函数提供了一个不重名的归属。**

这解决了一个大程序里迟早爆发的灾难：**名字不够用**。

一个几百个函数的程序，想给每个函数都起一个全局唯一、绝不重复的名字，几乎不可能，而且会逼出一堆又长又丑的名字（`解析模块里的检查金额是否为正的函数`）。有了命名空间，每个抽屉各论各的，名字可以短小自然，**冲突交给"姓"去化解**。

> 命名空间是模块送的一份"隐形大礼"：
> 它让**成千上万的名字，在各自的地盘里和平共处**，互不踩踏。
> 没有它，大型程序的连接会淹没在无穷无尽的重名纠纷里。

---

## 2.8　回到 CoinJar：拆出"账目模块""计算模块""显示模块"

理论备齐，我们正式给 CoinJar 做一次"部门重组"。

上一章它是一张平铺的函数小网。现在，我们把这些函数收进几个清清楚楚的抽屉，并用导入/导出把它们连起来。

**① 格式化模块**（最底层的地基，谁都能借，它谁也不靠）：

```
模块 格式化:
    导出 格式化金额(金额):  返回 "¥" + 保留两位小数(金额)
    导出 格式化日期(日期):  返回 ...
    保留两位小数(数字):     返回 ...      # 内部私货，不导出
```

**② 账目模块**（管"一笔账"长什么样、怎么解析、怎么检查）：

```
模块 账目:
    导出 解析输入(文本):
        ...
        返回 { "项目": 项目, "金额": 金额 }

    导出 检查账目(账目):
        如果 账目["金额"] <= 0: 返回 假
        返回 真
```

**③ 计算模块**（管所有和钱有关的运算，借用了格式化）：

```
模块 计算:
    从 格式化 导入 格式化金额        # ← 依赖：计算 → 格式化

    导出 算服务费(账目):  返回 账目["金额"] * 0.06
    导出 算总额(所有账目): 返回 ...
```

**④ 显示模块**（管所有给用户看的东西，借用了格式化）：

```
模块 显示:
    从 格式化 导入 格式化金额        # ← 依赖：显示 → 格式化

    导出 报告结果(账目):
        漂亮金额 = 格式化金额(账目["金额"])
        显示("已记录：" + 账目["项目"] + " " + 漂亮金额)
```

**⑤ 总指挥**（把各部门接力串起来，依赖了几乎所有人）：

```
模块 主程序:
    从 账目 导入 解析输入, 检查账目
    从 计算 导入 算服务费
    从 显示 导入 报告结果

    导出 记一笔账(输入文本):
        账目 = 解析输入(输入文本)            # 喊"账目"部门
        如果 检查账目(账目) == 假:
            显示("金额不对"); 返回
        账目["服务费"] = 算服务费(账目)      # 喊"计算"部门
        报告结果(账目)                       # 喊"显示"部门
```

退一步看这整张图，CoinJar 已经脱胎换骨了。它不再是"一堆函数"，而是**五个各司其职的部门，靠一条条清晰的导入专线连成的一张网**：

- 每个部门**对外只露一小块接口**，内部细节自己藏着；
- 部门之间**谁依赖谁，一目了然**（看 `导入` 那几行就知道）；
- 想改某个部门的内部，只要**接口不变**，别的部门稳如泰山。

上一章我们解决了"函数怎么连"，这一章我们解决了"函数太多怎么归类、归类后的抽屉怎么连"。CoinJar 的连接，完成了从"细线"到"粗线"的第一次升级。

---

## 2.9　本章小结与断点预告

**这一章，我们把连接放大了一号：**

- 函数太多、平铺一地会带来新麻烦：找不着、看不清关系、容易撞名。
- **模块**是解药——它是个"带标签的抽屉"，把**一组相关函数打包**起来。
- **导出 / 导入**是模块之间的连线：一个抽屉开放工具，另一个抽屉来借。
- **接口与实现**的分离是模块化的灵魂：连接接在"承诺"上，而非"做法"上——实现可换，接口不动。
- **依赖图**画出了模块的骨架；依赖最好单向流动，**切忌绕圈（循环依赖）**。
- **命名空间**让成千上万的名字在各自地盘里和平共处。
- CoinJar 从一张函数小网，重组成了**五个部门连成的网**。

**但是，断点又来了——而且这次是个很"实在"的问题。**

我们一直在说"模块"、说"抽屉"、画依赖图……可你有没有发现，这些**全是概念**？

`解析` 模块、`计算` 模块、`格式化` 模块——它们到现在为止，都还只是我们脑子里、纸面上的划分。

> **这些模块，在你的硬盘上，到底是个什么东西？**
> 它们存在哪？是一坨写在一起、只是用"模块"两个字隔开的文字吗？还是各自有各自的"身体"？

是时候让这些飘在概念里的模块，**落到真实的磁盘上**，变成你能用鼠标点开、能看到图标的**文件**了。

> **下一站，第三部：文件与文件。**
> 我们将看到，模块如何从一个抽象的概念，变成硬盘上一份份实实在在的源文件，以及——这些散落在硬盘各处的文件，又是怎么互相"找到对方"的。
